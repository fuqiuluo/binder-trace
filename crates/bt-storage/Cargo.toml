[package]
name = "bt-storage"
version.workspace = true
edition.workspace = true

[lib]
name = "bt_storage"

[dependencies]
bt-decoder = { path = "../bt-decoder" }

[dev-dependencies]
bt-common = { path = "../bt-common" }
